API_PORT=4000
API_HOST=0.0.0.0
API_CORS_ORIGIN=http://localhost:3000

# Supabase (server-side)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Redis (fast state machine)
REDIS_URL=
# TTLs (seconds)
SESSION_STATE_TTL_SECONDS=3600
BAN_STATE_TTL_SECONDS=2592000

# Internal job (cron): secret for GET /internal/run-scheduler
INTERNAL_SCHEDULER_SECRET=

# HMAC: same secret as client (Wasm) for X-Signature on POST /v1/edr/log
HMAC_SECRET=
